from django.urls import path, include
from physician.views import *

urlpatterns = [
    # path('account/', include('patient.suburls.account_url')),
    # path('fixed', Fixed.as_view()),
    # path('physicians', get_physicians, name='get_physicians'),

]